# 金刚琢 开发进度

> 更新日期:2026-09-15
> 产品版本:0.4.1（persist v8）
> 对照基线:`docs/requirements.md` v0.2（历史草案）+ `docs/分身Agent规划.md`（分身与顶栏）+ 当前代码
> 状态图例:✅ 已完成 / ⚠️ 部分完成 / ❌ 未实现 / — P2 未细究

---

## 一、总览

| 优先级 | 总数 | ✅ 已完成 | ⚠️ 部分完成 | ❌ 未实现 |
| --- | --- | --- | --- | --- |
| P0(MVP 必须) | 67 | 52 | 15 | 0 |
| P1(首版建议) | 26 | 13 | 7 | 6 |
| P2(后续规划) | 10 | — | — | 基本未动,不影响首版 |

**产品目标**：一个窗口里完成文件管理器、编辑器、终端和多个 Agent。内置 Claude Code / Codex / OpenCode / Antigravity / Hermes；Claude、Codex 可复制分身（独立 Key、URL、模型、权限，历史共用默认家目录）；设置里可加自定义 CLI。

**结论**：v0.2 P0 与分身主路径已落地，可日常使用。分身 S0～S5 主路径已实现，**2026-09-14 用户确认实机测试通过**；2026-09-15 修正 Codex 分身会话切回原版时的 provider 路由，并确认 Claude Code 原版/分身按入口隔离配置。仍弱的是按工作区隔离布局/打开文件（WSP-004/005）、另存为、工作区标签页、Skill Git 导入、更新包签名与自动安装。空闲一键全清未对真实家目录代跑。

---

## 二、历史已修（保留备查，不再当缺口）

### 2026-08-15

上午一轮：TRM-006 孤儿终端、凭据不进渲染层、工作区白名单、transcript 节流、回收站、外链协议、Skill 50MB、文档版本对齐。

下午一轮（产品目标：集成工作台 + 可扩展 Agent）：

1. **Agent 适配器** — `src/lib/agents.ts`。新增内置 Agent 只追加一条；设置可添加自定义 CLI（AIT-009）。
2. **Hermes** — 内置第三 Agent；顶栏 / 启动栏 / 向导 / 历史筛选同步。
3. **Claude 危险启动** — 启动面板可选 default / Auto / `--dangerously-skip-permissions`。Hermes 对应 YOLO（`--yolo`）。
4. **配置方案编辑 UI**（AIT-004/006、CFG-001/002、ENV-004）— 命令、参数、模型、终端策略、环境变量、浏览 exe。
5. **会话记录** — 右栏查看正文、收藏/删除、按 Agent 筛选、磁盘历史含 Hermes 根目录。
6. **收藏/最近/重命名** — 左栏真数据；工作区与终端可重命名。
7. **文件** — 树形视图、拖入终端、复制路径/副本、fs.watch、外部修改冲突、面包屑跳转。
8. **编辑预览并排、终端分屏、快捷键自定义、Shell 类型**。

### 2026-08-29

**HIS-012 历史标题**：本机和磁盘历史列表优先显示各 Agent 自己总结的标题（Claude `ai-title`、Codex `session_index.jsonl` 的 `thread_name`、OpenCode `title`），不再用终端 TUI 启发式当标题。

### 2026-07-14（仍有效）

磁盘双击打开、空目录态、Claude PATH 解析、credentialSet 水合、磁盘历史搜索、PromptModal/ConfirmModal、工作区右键关闭、Claude Auto 模式。

### 2026-09 分身与文件树

- Claude / Codex **分身**：独立 Key/URL/模型/权限；历史身份 `family + nativeSessionId`；继续默认上次入口；缺 Key 全入口拦截；顶栏最多 4 个具名入口 +「更多」；隐藏同时作用于顶栏和更多。Claude 分身用 `--settings` 覆盖 `~/.claude/settings.json` 的 env，避免家目录设置盖掉进程注入。Codex 分身 provider 仅写入独立 profile，不再修改用户主 `config.toml`，避免影响 Codex 桌面版的官方模型列表；启动时会清理 0.4.1 曾写入的带归属标记兼容段。使用原版 Codex 继续或分叉分身会话时，启动参数显式覆盖为官方 `openai` provider，避免把 ChatGPT 登录令牌错误发送到 Platform API。创建/编辑可拉取模型列表。详见 `docs/分身Agent规划.md`。
- 文件树右键：复制/剪切/粘贴（含跨目录移动）、复制绝对/相对路径、把路径插入当前终端。
- v0.4.1 修复树节点右键菜单把节点名重复拼入路径的问题；打开、剪切/复制、粘贴到目录、重命名、删除、设为工作区、插入/复制路径和资源管理器定位统一使用同一目标路径契约，右键节点时同步选中。
- 会话历史按目录分组，继续/分叉走原生 session。详见 `docs/会话历史与恢复改造.md`、`docs/历史会话目录分组.md`。
- GitHub 检查更新：免安装认 zip、安装版认 nsis exe；点下载只打开浏览器。发版附件必须 ASCII 名，见 `docs/发版打包.md`。

自测：`npm run typecheck` + `npm test`（2026-09-15：51 files / 237 tests）；`npm run electron:build` 已生成 0.4.1 ZIP 与 NSIS 安装包，ZIP 已验证可解压并在无提权情况下删除。

---

## 三、仍须优先处理（半截或 P1）

1. **WSP-004/005** — 打开的文件未持久化；布局全局共享，切换工作区不保留各自终端/文件。
2. **EDT-002 另存为** — 有保存，无另存为对话框。
3. **SKL-008** — Skill 从 Git 仓库导入未做。
4. **UPD-003/004** — 无代码签名，也无下载后自动安装重启。
5. **WSP-007** — 无工作区标签页（TopBar 下拉）。

---

## 四、详细清单（按章节）

> 2026-09-15 已按当前代码重核。历史草案编号仍对照 `docs/requirements.md` v0.2；分身不另起编号，见文末 6.14。

### 6.1 应用启动与环境检测

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| ENV-001 | P0 | ⚠️ | FirstRunWizard 检测全部内置 Agent CLI；尚未检测 Windows 版本和 PowerShell |
| ENV-002 | P0 | ✅ | defaultTool='claude',Claude Code 入口在第一位 |
| ENV-003 | P0 | ⚠️ | 显示「未安装」；无检测路径、无「重新检测」按钮 |
| ENV-004 | P0 | ✅ | ToolProfile 可自动使用 PATH，也可在设置中浏览并指定可执行文件路径 |
| ENV-005 | P0 | ✅ | Electron 原生运行,默认 powershell.exe |
| ENV-006 | P1 | ✅ | 设置可选 powershell.exe / cmd.exe / bash.exe |

### 6.2 工作区管理

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| WSP-001 | P0 | ✅ | 文件夹选择器 + 磁盘双击打开 |
| WSP-002 | P0 | ✅ | 右键关闭；左栏可重命名工作区 |
| WSP-003 | P0 | ⚠️ | lastOpenedAt 已记录；工作区列表仍按插入序；「最近」区是真实最近文件 |
| WSP-004 | P0 | ⚠️ | sessions/profiles/terminals/layout 持久化；打开的文件未持久化，布局全局共享非按工作区 |
| WSP-005 | P0 | ⚠️ | switchWorkspace 只切 activeId；打开文件/终端/布局全局共享 |
| WSP-006 | P1 | ⚠️ | 工作区可收藏；无颜色/图标自定义 |
| WSP-007 | P1 | ❌ | 无工作区标签页,TopBar 是下拉切换 |

### 6.3 文件管理

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| FIL-001 | P0 | ✅ | grid/list/detail + **树形**展开/折叠；有刷新与面包屑 |
| FIL-002 | P0 | ✅ | 新建/重命名/删除；剪切/复制/粘贴（含跨目录移动，同名剪切失败） |
| FIL-003 | P0 | ✅ | 删除走 `shell.trashItem`（回收站） |
| FIL-004 | P0 | ✅ | 内容/文件名搜索(SearchView + electron/search.ts) |
| FIL-005 | P0 | ✅ | revealInExplorer；工具栏与右键可复制绝对/相对路径 |
| FIL-006 | P0 | ✅ | 终端 onDrop，拖入文件写入转义路径 |
| FIL-007 | P0 | ✅ | `electron/watch.ts` fs.watch，外部修改刷新文件树 |
| FIL-008 | P1 | ⚠️ | .gitignore 解析有；自定义排除规则不可配(ALWAYS_IGNORE 硬编码 .git) |

### 6.4 编辑器与预览

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| EDT-001 | P0 | ✅ | Monaco,识别 ts/js/css/json/md/html |
| EDT-002 | P0 | ⚠️ | 多标签/dirty/Ctrl+S/撤销重做有;**缺另存为** |
| EDT-003 | P0 | ✅ | fs.watch + mtime 比对；外部修改且未保存时提示冲突 |
| EDT-004 | P0 | ✅ | markdown/json/text/image 预览 |
| EDT-005 | P0 | ✅ | 可「并排预览」 |
| EDT-006 | P1 | ✅ | PDF(iframe)/HTML(DOMPurify)/CSV(表格) |
| EDT-007 | P1 | ✅ | GitView 逐文件 diff;非 Git 仓库有提示 |
| EDT-008 | P2 | — | 未实现 Office 预览 |

### 6.5 终端

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| TRM-001 | P0 | ✅ | node-pty + ConPTY,默认 powershell(已修 PATH 解析) |
| TRM-002 | P0 | ✅ | 创建/关闭/切换/重命名 |
| TRM-003 | P0 | ✅ | cwd = 工作区根目录 |
| TRM-004 | P0 | ✅ | 终端左右分屏（当前为 50/50，非任意横/纵嵌套） |
| TRM-005 | P0 | ⚠️ | 仅 AI 终端关闭时确认;普通 shell 有进程也不确认 |
| TRM-006 | P0 | ✅ | 终端元数据持久化；重启后 tab 为孤儿，进程不自动恢复 |
| TRM-007 | P1 | ⚠️ | 复制/粘贴靠 xterm 内置;scrollback 硬编码 5000 不可配;**无内容查找** |
| TRM-008 | P1 | ⚠️ | 仅进程退出时通知;**未检测命令结束/等待输入** |

### 6.6 AI 工具启动与配置方案

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| AIT-001 | P0 | ✅ | 顶栏最多 4 个具名入口 +「更多」；含内置 Agent 与分身；隐藏同时作用于顶栏和更多 |
| AIT-002 | P0 | ✅ | createSession + newTerminal('ai') |
| AIT-003 | P0 | ⚠️ | terminalStrategy 字段有,但总 newTerminal,**无复用空闲终端** |
| AIT-004 | P0 | ✅ | 设置「配置方案」可编辑命令/参数/模型/环境/权限 |
| AIT-005 | P0 | ✅ | 种子默认 Claude Code |
| AIT-006 | P0 | ✅ | 设置可复制/编辑/删除 profile；分身删除检查运行中终端 |
| AIT-007 | P0 | ✅ | 已去掉 LaunchModal；点入口直接启动，缺 Key 拦截（原版仍可走 CLI 登录） |
| AIT-008 | P0 | ⚠️ | 退出码写入终端;**终端无失败后「重新运行」按钮** |
| AIT-009 | P1 | ✅ | 设置可添加自定义 CLI；Claude/Codex 可复制分身 |
| AIT-010 | P1 | ⚠️ | 有「插入所选」「插入路径」;**缺插入当前文件全文/错误日志** |
| AIT-011 | P0 | ✅ | getProfileForTool 优先 workspace scope |

### 6.7 历史对话与会话切换

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| HIS-001 | P0 | ✅ | 启动时 createSession；PTY 输出节流写入 transcript |
| HIS-002 | P0 | ⚠️ | 按 lastActiveAt 倒序；已按目录分组，卡片不单独标工作区名 |
| HIS-003 | P0 | ⚠️ | 搜索代码有；正文开始写入后可搜，旧数据仍可能空 |
| HIS-004 | P0 | ⚠️ | 有工具筛选与目录分组;**缺日期筛选** |
| HIS-005 | P0 | ✅ | 右侧可查看清理后的正文 |
| HIS-006 | P0 | ✅ | 重命名/收藏/删除本机记录（不删原生磁盘历史） |
| HIS-007 | P0 | ✅ | deleteSession 只删本机记录；右栏有删除入口 |
| HIS-008 | P0 | ✅ | 有原生 session ID 则可继续；无 ID 明确不可继续 |
| HIS-009 | P0 | ✅ | 继续会话 / 基于此记录新建（分叉），语义已按规格区分 |
| HIS-010 | P0 | ✅ | hydrate 时 running → interrupted |
| HIS-011 | P0 | ✅ | 凭据存 Credential Manager,transcript 不含凭据 |
| HIS-012 | P1 | ✅ | 列表优先 Claude ai-title / Codex thread_name / OpenCode title |
| HIS-013 | P1 | ✅ | exportSession 导出 Markdown |
| HIS-014 | P1 | ⚠️ | 右栏历史面板有；固定/多会话快捷切换仍可加强 |
| HIS-015 | P1 | ✅ | 磁盘 jsonl 搜索 + 原生 --resume |

### 6.8 模型与凭据配置

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| CFG-001 | P0 | ✅ | 设置可编辑 model |
| CFG-002 | P0 | ✅ | 设置可编辑 command/args/envs |
| CFG-003 | P0 | ✅ | @napi-rs/keyring；明文只在主进程注入 PTY |
| CFG-004 | P0 | ✅ | 显示「已设置/未设置」+ 掩码 |
| CFG-005 | P0 | ✅ | workspace scope 覆盖 global；设置可编辑 |
| CFG-006 | P0 | ✅ | 敏感 env 掩码,退出码不含环境变量 |
| CFG-007 | P1 | ✅ | 导出(敏感置空)/导入(新 id；分身换新身份) |

### 6.9 Skill 管理

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| SKL-001 | P0 | ✅ | scope 'global'/'workspace' + 筛选 |
| SKL-002 | P0 | ✅ | 本地文件夹导入(skill:import) |
| SKL-003 | P0 | ✅ | 名称/来源/作用域/指纹(SHA-256)/更新时间 |
| SKL-004~007,009,010 | P2 | — | 未实现(兼容判断/同步/市场) |
| SKL-008 | P1 | ❌ | Skill.source 有 'git' 但只支持本地文件夹导入,**无 Git 仓库导入** |

### 6.10 快捷操作

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| QCK-001 | P0 | ✅ | 命令面板(CommandPalette + COMMANDS 清单) |
| QCK-002 | P0 | ✅ | Ctrl+P/Ctrl+Shift+F/Ctrl+Shift+T/Ctrl+Shift+H |
| QCK-003 | P0 | ⚠️ | 设置可改 keymap；**无冲突检测** |
| QCK-004 | P1 | ❌ | 未实现 Prompt/命令模板收藏 |
| QCK-005 | P1 | ❌ | 无 globalShortcut 全局唤起 |
| QCK-006 | P1 | ❌ | 无从剪贴板创建文件/插入内容 |

### 6.11 Git 能力

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| GIT-001 | P1 | ✅ | 分支/ahead/behind/文件状态 |
| GIT-002 | P1 | ⚠️ | 逐文件 diff 有;**缺整体 Diff 视图** |
| GIT-003 | P1 | ✅ | 暂存/取消暂存/提交,提交前 showConfirm 展示文件清单 |
| GIT-004 | P2 | — | 仅 gitCheckout;无分支创建/拉取/推送 |

### 6.12 布局与外观

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| UI-001 | P0 | ✅ | 左右/上下 Splitter 拖拽改尺寸 |
| UI-002 | P0 | ⚠️ | 可隐藏/恢复;**缺临时最大化** |
| UI-003 | P0 | ✅ | setPointerCapture + RAF,拖拽不重叠不跳动 |
| UI-004 | P0 | ✅ | dark/light/system + matchMedia 监听 |
| UI-005 | P0 | ✅ | Electron 原生 DPI 缩放 |
| UI-006 | P0 | ⚠️ | 全局 layout 持久化;**非按工作区保存** |
| UI-007 | P1 | ✅ | 保存/应用/删除命名布局 |
| UI-008 | P0 | ✅ | CSS var(--token) + data-theme 切换 |

### 6.13 应用更新

| 编号 | 优先级 | 状态 | 说明 |
| --- | --- | --- | --- |
| UPD-001 | P1 | ✅ | 启动 8s 后后台查 GitHub Release；设置可手动检查 |
| UPD-002 | P1 | ✅ | 有新版本提示，并打开当前渠道对应包装（zip / nsis exe）或发布页。附件必须 ASCII 名 `RingCode-<ver>-x64.zip/.exe`，发版步骤见 `docs/发版打包.md` |
| UPD-003 | P1 | ❌ | 无代码签名与安装前校验 |
| UPD-004 | P2 | — | 未实现 |

### 6.14 分身与快速启动（规划项，非 v0.2 编号）

主路径已落地，**2026-09-14 用户确认实机通过**；2026-09-15 已完成 Codex/Claude 双向续聊路由复核。细节以 `docs/分身Agent规划.md` 第 11、12 节为准。

- 分身只从原版 Claude / Codex 复制；配置与会话分离；不隔离家目录、不改用户主 `config.toml`。
- 缺 Key：分身全入口拦截；原版可走 CLI 登录。
- Claude 分身 `--settings` 覆盖家目录 `settings.json` 的 env，原版不加载分身 settings；Codex 分身使用独立 overlay，原版 resume/fork 显式覆盖为官方 `openai` provider，不改主配置。
- 删除 / 清 Key / 一键全清：运行中 PTY 拦截；全清保留分身设置。空闲全清未对真实家目录代跑。
- 顶栏显隐与「更多」收纳；隐藏不影响历史、命令面板、快捷键。

---

## 五、未完成项汇总(按优先级,方便以后接手)

### P0 部分完成 — 不挡日常使用

WSP-004/005（按工作区隔离打开文件与布局）、EDT-002（另存为）、AIT-003（复用空闲终端）、AIT-008（失败后重新运行）、QCK-003（快捷键冲突检测）、ENV-001/003（系统检测与重新检测）、HIS-002/003/004（工作区名/旧数据搜索/日期筛选）、TRM-005、UI-002/006。

### P1 未完成

WSP-007（多工作区标签页）、SKL-008（Git 导入 Skill）、QCK-004/005/006、UPD-003/004（签名与自动安装）。

### P1 部分完成

WSP-006（收藏有、无颜色图标）、GIT-002（整体 Diff）、TRM-007（查找/可配 scrollback）、TRM-008（命令结束通知）、AIT-010（插入全文/错误日志）、HIS-014（固定历史面板）、FIL-008（自定义排除）。

---

## 六、已决事项

见 `docs/requirements.md` §14（2026-08-15 关闭）与 `docs/分身Agent规划.md`。GitHub 仓库为 [diaoerlangdang/RingCode](https://github.com/diaoerlangdang/RingCode)。发版与 GitHub 附件命名见 `docs/发版打包.md`。
